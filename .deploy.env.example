# Optional: copy to .deploy.env and fill values manually.
# If omitted, deploy script will try to read host/user/port/path from .vscode/sftp.json.
DEPLOY_USER="user"
DEPLOY_HOST="server"
DEPLOY_PATH="/path/to/site"

# Optional SSH port, uncomment if not 22.
# DEPLOY_PORT="22"
